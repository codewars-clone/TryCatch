import React from 'react'
const NextButton = () => {
  return ( 
    <div  id='next' className="buttons">
      <button className='button is-danger'>
        <i class="fas fa-forward"></i>
        NEXT
      </button>
    </div>
  );
}

export default NextButton;