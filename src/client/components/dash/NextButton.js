import React from 'react';
const NextButton = () => {
  return (
    <div className="buttons">
      <button id="next"  className="button is-rounded is-large is-danger">
        <i className="fas fa-times"></i>
      </button>
    </div>
  );
};

export default NextButton;
